import { html } from 'lit-html';
import { Canvas, Meta, Story, ArgsTable, PRIMARY_STORY } from '@storybook/addon-docs';

import { CssPropsBlock } from "@ljcl/storybook-addon-cssprops";

import {
  themeSchemeDecorators
} from "$lib-mylib/.storybook/src/decorators";

import { args, argTypes, cssProperties } from './story/story-args';

import './story/scss/mylib-icon.stories.scss';

import {
  meta,
  Template,
  TemplateIconList,
  TemplateCustomIconFont,
  TemplateSVG,
  TemplateCustomColor,
  TemplateSvgList
} from './story/helpers';

<Meta
  title={meta.title}
  component='mylib-icon'
  parameters={meta.parameters}
/>

# Icon

<Section>
- [Overview](#overview)
- [Props](#props)
- [Events](#events)
- [CSS Props](#css-props)
- [Usage](#usage)
- [Featurettes](#featurettes)
- [Use cases and examples](#use-cases-and-examples)
</Section>

<DISWRefVersionBadge refName="DI SW Icon Reference" version="11.1.0" refUrl="http://uxhub.net.plm.eds.com/docs/di_sw_icon_reference/icon_reference/iconography/" />

## Overview
Icon is an individual UI element that is used to either execute a command or visually represent an object.

<Canvas withToolbar>
  <Story
    name="Overview"
    args={{...args}}
    argTypes={{...argTypes}}
    cssprops={{...cssProperties}}>
    {Template.bind({})}
  </Story>
</Canvas>

<SectionDivider />

## Props
<ArgsTable story={PRIMARY_STORY} />

<SectionDivider />

## CSS Props
<CssPropsBlock />

<SectionDivider />

## Usage
**(NONE)**

<SectionDivider />

## Featurettes

### Color Scheme (Light / Dark)

<Story
  name="Color Scheme (Light / Dark)"
  args={{...args}}
  argTypes={{...argTypes}}
  cssprops={{...cssProperties}}
  decorators={themeSchemeDecorators}>
  {Template.bind({})}
</Story>

### Icon List
<Story
  name="Icon List"
  args={{
    size: 'md'
  }}
  argTypes={{
    size: argTypes.size
  }}>
  {TemplateIconList.bind({})}
</Story>


### SVG Embeded Library - Icon
<Canvas>
  <Story
    name="SVG Embeded Library - Icon"
    args={{
      name: 'AppIcon32_placeholder',
      size: 'md',
      type: 'svg'
    }}>
    {Template.bind({})}
  </Story>
</Canvas>


### SVG Embeded Library - Icon List
<Story
  name="SVG Embeded Library - Icon List"
  args={{
    size: 'md'
  }}
  argTypes={{
    size: argTypes.size
  }}>
  {TemplateSvgList.bind({})}
</Story>

### Custom Icon Font-Family
<Canvas>
  <Story
    name="Custom Icon Font-Family"
    args={{
      name: 'face',
      size: 'md'
    }}
    argTypes={{
      size: argTypes.size
    }}>
    {TemplateCustomIconFont.bind({})}
  </Story>
</Canvas>

### SVG Content Icon
> Note the usage of the attribute `slot` in the svg tag.
<Canvas>
  <Story
    name="SVG Content Icon"
    args={{
      size: 'md'
    }}
    argTypes={{
      size: argTypes.size
    }}>
    {TemplateSVG.bind({})}
  </Story>
</Canvas>

<SectionDivider />

## Use cases and examples

<Canvas withToolbar>
  <Story
    name="Custom Icon Color"
    args={{
      size: 'md'
    }}
    argTypes={{
      size: argTypes.size
    }}>
    {TemplateCustomColor.bind({})}
  </Story>
</Canvas>

